#include <stdio.h>

int main() {
    int num1, num2;
    scanf("%d %d", &num1, &num2);
    if ((a>0 && b<0) || a<0 && b>0) {
    printf("True\n");
    } else {
        printf("False\n");
    }
  
    return 0;
}